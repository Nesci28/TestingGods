import { ControlsDirective } from './controls.directive';

describe('ControlsDirective', () => {
  it('should create an instance', () => {
    const directive = new ControlsDirective();
    expect(directive).toBeTruthy();
  });
});
